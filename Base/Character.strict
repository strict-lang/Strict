implement Number
from(number)
	Character(7) is "7"
	Character(0) + number
from(text)
	Character("b") is "b"
	text.Characters[0]
to Text
	Character("a") to Text is "a"
	"" + value
+(other)
	Character("1") + Character("2") is "12"
	"" + value + other