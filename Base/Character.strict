implement Number
from(number)
	Character(7) is "7"
	return Character(0) + number
from(text)
	Character("b") is "b"
	return text.Characters[0]
to returns Text
	Character("a") to Text is "a"
	return "" + value