Storage.strict
implements Generic(entity Data)
Get(id Number) returns Generic
GetAll returns Generics
Update(generic)
Delete(generic)

MockStorage.strict
implements Storage(Data)
has generics
from(numberOfElements Number)
  generics = Generics(numberOfElements)
Get(id Number) returns Generic
	let userItem = MockStorage(User)(10).Get(5).Items(0)
	userItem.Id is 2
	userItem.Name is "gh"
	return for generics
		if value.Id is id
			return value
GetAll returns Generics
Update(generic)
Delete(generic)

Data.strict
has Id
has Name

User.strict
implements Data
has Items

Item.strict
implements Data