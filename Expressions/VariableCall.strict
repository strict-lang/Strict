has variableName Text
has variableTypeName Text
to Text
	VariableCall("count", "Number") to Text is "count"
	variableName
Parse(name Text, typeName Text) VariableCall
	Parse("count", "Number").variableName is "count"
	VariableCall(name, typeName)
