namespace Strict.Optimizers;

public class Class1; //TODO: still need to work on this, this should be Runtime level optimizations, up to this point we just had Validators, constant collapsing, tests execution, etc. at runtime we want only the code that is needed for execution, so it will be much less, but still we want to optimize it!