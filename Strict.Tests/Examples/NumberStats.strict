has logger
Run
	constant numbers = (10, 20, 30, 40, 50)
	constant total = numbers.Sum
	mutable maximum = 0
	for numbers
		if value > maximum
			maximum = value
	logger.Log("Sum: " + total)
	logger.Log("Maximum: " + maximum)
